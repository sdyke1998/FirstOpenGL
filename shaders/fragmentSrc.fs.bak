#version 460 core

in vec2 texCoord;

out vec4 nigger;

uniform sampler2D ourTex;
uniform sampler2D nexTex;
uniform float ratio;

void main(){

nigger = mix(texture(ourTex, vec2(-texCoord.s, texCoord.t)), texture(nexTex, texCoord), ratio);
}